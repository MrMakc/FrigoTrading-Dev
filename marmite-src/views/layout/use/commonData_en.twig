{% set dataFeLanguage = dataFeLanguage|default('en') %}
{% set dataFeLongLanguage = dataFeLongLanguage|default('en-GB') %}
{% set dataFeDateFormatJs = dataFeDateFormatJs|default('mm/dd/yy') %}
{% set dataFeDateFormatTwig = dataFeDateFormatTwig|default('m/d/Y') %}
{% set dataFeDateFormatLabel = dataFeDateFormatLabel|default('mm/dd/yyyy') %}
{% set pageTitle = pageTitle | default('Styleguide - ' ~  projectRealName  ~ ' - Agence Acti') %}
{% set pageTemplate = pageTemplate | default('') %}
{% set pageClass = pageClass | default('') %}

{% set defaultRequiredMsg = defaultRequiredMsg | default('This field is required.') %}
{% set defaultErrorMsg = defaultErrorMsg | default('This field is unvalid') %}
{% set defaultPwdErrorMsg = defaultPwdErrorMsg | default('Your password and confirmation password must be the same.') %}

{% set images_sizes = {
  exemple: {w: 500, h: 500},
} %}
